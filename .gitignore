node_modules/
out/
dist/
*.dmg
*.zip
.DS_Store
.env
.env.local

# Notas de release que genera CI desde CHANGELOG.md (scripts/prepare-release.sh)
build/release-notes.md

# Agent emit-results legacy (proyectos pre-rebrand; .gravity/results/ ya lo cubre
# su propio .gitignore anidado, igual que clipboard-images/)
.iaterminal/results/

# Worktrees de agentes (estado local)
.covenant/

# Mockups del visual companion (brainstorming)
.superpowers/

# Worktrees de git creados por la orquestación de agentes (Fase 1 git worktree)
/.gravity/worktrees/

# Helper nativo de dictado (se compila con npm run build:mac-dictation)
/native/mac-dictation/gravity-mac-dictation
